import React from "react"
import ReactDOM from "react-dom"
import DiscreteSlider from "./Corousel"

ReactDOM.render(
  <React.StrictMode>
    <DiscreteSlider />
  </React.StrictMode>,
  document.getElementById("root")
)
